---
id: PI-06
epic: 020-product-integrity
title: Diagnose credit-card-payment contributions in Sankey
status: done
dependencies: []
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Reproduce the observed “Credit card payments”/equivalent Sankey contribution
  against an identified application commit and reporting period without copying
  private transaction data into the repository.
- [x] Trace every relevant contributing row through effective category, posting
  state, linked accounts, transfer pair state, counterpart availability/history,
  shared expense expression, Sankey grouping, and Activity evidence.
- [x] Classify each row as confirmed transfer, unconfirmed candidate, undetected
  transfer, missing linked counterpart/history, or shared-accounting regression;
  separate observed facts from hypotheses.
- [x] Verify with a hand-checkable fixture that a correctly confirmed checking-to-
  linked-credit-card repayment contributes zero income/spending and no Sankey
  expense category while the original card purchase contributes exactly once.
- [x] Make unresolved-versus-confirmed behavior explicit: an unresolved payment
  remains counted under current conservative policy unless the accepted resolution
  changes that policy deliberately.
- [x] Produce a bounded defect/workflow recommendation for PI-07, including the
  concrete regression to add. If current production-like evidence is unavailable,
  document the exact safe diagnostic query/workflow needed rather than declaring
  the accounting path correct or incorrect.
- [x] Run existing transfer, accounting, Sankey, and full-evidence drilldown tests;
  record commands, commit, and results.

## Technical Context

`analytics_cashflow_sankey()` in `app/api/routes.py` already calls
`posted_activity(..., include_transfers=False)` and sums the shared
`expense_amount()` expression from `app/services/accounting.py`. A category such as
Plaid `LOAN_PAYMENTS` maps to `FINANCE/LOANS`; displayed wording may also reflect a
manual or rule category. Transfer state lives in `TransferPair`, and Activity exposes
`is_transfer` versus `is_transfer_candidate`. Existing tests prove exclusion for
monthly/cashflow endpoints but do not explicitly assert the linked-card case against
the Sankey category payload.

Suggested role: accounting/debugging engineer. Save findings under
`artifacts/PI-06-card-payment-sankey-audit.md`; this task diagnoses and specifies,
but does not begin with a speculative fix.

## Execution Log

- 2026-09-08: Task created after inspection confirmed shared accounting is already
  used by Sankey; concrete contribution audit pending.
- 2026-09-08: Completed code/fixture audit at integrated commit `2010806` and
  saved [PI-06 audit](../artifacts/PI-06-card-payment-sankey-audit.md). The local
  checkout contains no private ledger/deployment target, so the observed rows
  cannot be truthfully classified here. The artifact records the exact safe
  operator workflow and read-only query rather than guessing. Confirmed linked
  transfers are excluded by the shared scope; unresolved candidates and unpaired
  rows remain counted conservatively. Focused transfer/accounting/Sankey/evidence
  checks: 18 passed. PI-19 must freeze the concrete observed state after that
  workflow supplies it; PI-09 may begin its design exploration without assuming
  a category-name accounting defect.
- 2026-09-09: Re-ran the audit against the owner-operated local instance at
  `7c39934` for the exact selected 90-day period. All 13 Activity rows were
  inspected across three pages. The $16,746.41 category contribution comprises
  five unpaired repayments with linked card-side counterparts ($14,445.00) that
  current directed-date detection rejects, plus three outflows with no counterpart
  in available linked history ($2,301.41). There were no confirmed pairs,
  unconfirmed candidates, or shared-accounting regressions, so no confirmation or
  data mutation was performed. Relevant backend verification: 106 passed; the
  audit now specifies PI-19's exact red fixture and bounds PI-07 to detection/review
  UX and history-coverage communication.
