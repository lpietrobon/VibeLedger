---
id: PI-16
epic: 020-product-integrity
title: Independently verify duplicate accounting TDD compliance
status: done
dependencies: [PI-04]
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Run PI-15's frozen tests unchanged on an identified PI-04 commit and derive a
  second literal check of canonical/duplicate aggregate effects.
- [x] Verify exclusion occurs exactly once in every promised consumer while both
  source records remain visible/auditable and reversal restores the prior result.
- [x] Challenge sync/relink/remove/orphan paths and legitimate same-value charges;
  confirm no relationship is attached to an unrelated imported record.
- [x] The verifier may fix product code and add separate adversarial tests. It may
  not modify PI-15's accepted test or fixture contract.
- [x] Escalate and leave the task blocked if PI-15 needs change; otherwise record
  commands, commit, code-only fixes, coverage impact, and verdict.

## Technical Context

Suggested role: independent accounting QA. This gate must finish before UI workflow
TDD begins so the client does not encode a provisional backend relationship.

## Execution Log

- 2026-09-08: Task created; blocked on PI-04.
- 2026-09-08: Started independent verification on PI-04 commit `e367205`.
  `python -m pytest -q tests/test_pi15_duplicate_tdd.py` passed unchanged with
  18 tests. The literal fixture arithmetic independently confirms 100 income,
  74 expense, 26 net before correction and 100 income, 49 expense, 51 net
  after excluding only Market B; reversal restores both rows and the original
  totals.
- 2026-09-08: Audited aggregate consumers, source visibility, reversal,
  provider replay/modification/removal, relink, startup orphan cleanup, and
  legitimate same-value charges. Found code-only gaps for currency conflicts,
  currency-change invalidation, and active duplicate participation in transfer
  or refund detection. Repaired production code and added independent
  adversarial tests in `tests/test_pi16_duplicate_verifier.py`; PI-15 files and
  PI-03 were not changed.
- 2026-09-08: Verification passed: PI-15 18 passed; PI-16 plus PI-15 22
  passed; full backend suite 252 passed; `git diff --check` passed; roadmap
  scanner reported 0 issues. Frontend checks were unavailable because
  `frontend/node_modules` is absent and package installation was blocked by
  the environment. Final verdict: PASS. Evidence:
  [PI-16 independent verification](../artifacts/PI-16-duplicate-verification.md).
