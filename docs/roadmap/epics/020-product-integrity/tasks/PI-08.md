---
id: PI-08
epic: 020-product-integrity
title: Improve Sankey label readability
status: done
dependencies: [PI-21]
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Sankey node/category labels and amounts are materially easier to read at the
  supported desktop and narrow layouts than the current 10px presentation.
- [x] Longer effective-category labels remain distinguishable without overlapping
  nodes, clipping essential text, or forcing a materially wider page.
- [x] Expansion, hover/title evidence, refund-credit explanation, and keyboard or
  pointer interactions remain usable; readability work does not change money or
  grouping semantics.
- [x] Verify representative short/long labels, many categories, income expansion,
  refund credits, savings, and deficit at narrow and wide viewport sizes.
- [x] Add or update focused rendering/layout assertions where stable, run frontend
  tests, typecheck, and build, and record a concise visual walkthrough with the
  tested viewport sizes.
- [x] Meet PI-21's locked observable readability/accessibility cases without
  changing them during implementation; escalate any needed test-contract revision.

## Technical Context

`frontend/src/components/finance/charts/SankeyChart.tsx` fixes the viewbox width at
640, uses `text-[10px]`, six-pixel label offsets, node padding 18, and a dynamic
height capped at 760. Inspect the rendered chart before deciding whether typography,
spacing, wrapping/truncation, or responsive height needs adjustment. Treat this as a
bounded visual defect unless evidence reveals a deeper layout issue.

Suggested role: frontend engineer with lightweight visual QA.

## Execution Log

- 2026-09-08: Task created from the current Sankey implementation; visual correction
  and verification pending.

### 2026-09-08 — TDD decomposition

PI-21 records the initial rendering and accessibility contract; PI-22 independently
checks it and may repair product code only.
- 2026-09-11: Implemented the bounded readability correction in
  `frontend/src/components/finance/charts/SankeyChart.tsx`: labels render at 12px
  with an 8-unit label gap, long labels use measured-viewBox-aware ellipsis plus
  full accessible/title evidence, and expandable nodes expose stable keys,
  accessible names, and Enter/Space keyboard activation. Stable node keys,
  monetary values, expansion, pointer handlers, Sankey titles, refund explanation,
  and accounting/grouping inputs are unchanged.
- 2026-09-11: PI-21 frozen suite passed unchanged at wide (1280px host) and narrow
  (360px host) fixtures: 6 passed. Existing Sankey rendering test passed; full
  frontend suite passed (63 tests), typecheck and build passed. Relevant Sankey,
  cashflow, refund, and card-payment backend tests passed (68 tests). See
  `artifacts/PI-08-sankey-readability.md`.
