---
id: PI-21
epic: 020-product-integrity
title: Pin Sankey readability and accessibility cases before visual changes
status: done
dependencies: []
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Record repeatable wide/narrow viewport fixtures with short/long labels, many
  categories, income expansion, refund credits, savings, and deficit.
- [x] Add failing, stable assertions for the agreed minimum label legibility,
  accessible names/activation, non-overlap or truthful truncation behavior, and
  preservation of monetary values/keys. Use visual snapshots only where they are
  deterministic and inspect the rendered output rather than relying on CSS intent.
- [x] Establish the baseline failure caused by the present tiny label treatment and
  save viewport/observation evidence under `artifacts/PI-21-sankey-readability-tdd.md`.
- [x] Freeze the reviewed observable contract for PI-08. Any later need to revise
  those assertions is escalated, not silently changed by implementers/verifiers.

## Technical Context

Use `SankeyChart`'s focused test plus a deterministic browser/DOM or SVG geometry
test strategy appropriate to the repository. This is a visual TDD boundary, not a
requirement for brittle pixel-perfect snapshots.

Suggested role: frontend test/UX engineer.

## Execution Log

- 2026-09-08: Task created; visual test strategy and baseline pending.
- 2026-09-11: Started the frozen visual/accessibility contract. The contract will
  use deterministic synthetic Sankey responses rendered at 1280px and 360px
  host widths, with DOM/SVG semantics and geometry assertions rather than pixel
  snapshots. No production code is in scope.
- 2026-09-11: Added the frozen suite at
  `frontend/src/components/finance/charts/SankeyChart-readability-tdd.test.tsx`
  and the privacy-safe contract at
  `artifacts/PI-21-sankey-readability-tdd.md`. It covers wide/narrow rendering,
  long and short labels, many categories, income/category expansion, refund
  credits, savings, deficit funding, stable keys, exact values, accessible
  names, and keyboard activation.
- 2026-09-11: Focused red command
  `cd frontend && npm test -- --run
  src/components/finance/charts/SankeyChart-readability-tdd.test.tsx` produced
  **6 failed, 0 passed**, as expected against the current 10px/no-key/no-role
  implementation. Existing frontend tests excluding PI-21 passed (**57 tests**);
  `npm run typecheck` and `npm run build` passed. Relevant Sankey backend tests
  passed (**17 tests**, 2 existing deprecation warnings).
- 2026-09-11: `git diff --check` passed and the roadmap scanner reported 0
  issues. PI-21 is complete and unblocks PI-08. PI-21's test and contract are
  frozen; any later contract revision must be escalated.
