---
id: PI-28
epic: 020-product-integrity
title: Automatically reconcile high-confidence linked-account transfers
status: todo
dependencies: [PI-27]
superseded_by: null
assigned_agent: null
created: 2026-09-13
---

## Acceptance Criteria

- [ ] Implement PI-27's frozen distinction between high-confidence automatic
  confirmation, review candidates, and abstention without changing its accepted
  tests or weakening existing manual-pair validation.
- [ ] Use one generic reconciliation path across linked institutions and account
  types. Keep account roles and provider details as evidence inputs rather than
  hard-coded product-specific accounting behavior.
- [ ] Automatically confirm only exact, mutually unique pairs that satisfy the
  frozen strong-evidence contract. Preserve candidate/review behavior for plausible
  but weaker pairs and leave ambiguous or incomplete movements unpaired and
  counted.
- [ ] Store enough structured decision evidence to explain why an automatic pair
  qualified and distinguish it from manual confirmation. Make automatic decisions
  visible and reversible through the existing transfer relationship workflow.
- [ ] Revalidate automatic decisions when sync adds, modifies, or removes source
  rows. Preserve explicit rejection memory and manual confirmations; do not allow
  refresh or redetection to silently resurrect a rejected pair.
- [ ] Keep confirmed-pair exclusion in the shared accounting path. Do not add a
  Sankey-only filter, single-row transfer override, category-name exclusion, or
  account-type whitelist.
- [ ] Make PI-27 pass unchanged, retain PI-19/PI-20 and epic 010 transfer contracts,
  and run focused detector, sync, API, aggregate, Activity, and frontend evidence
  tests plus the full backend/frontend suites, typecheck, build, roadmap scan, and
  `git diff --check`.

## Technical Context

Likely code areas are `app/services/transfer_detector.py`, automatic-pair lifecycle
inside `app/services/sync_service.py`, `TransferPair` persistence/migration in
`app/models/models.py` and `app/db/schema_patches.py`, transfer APIs in
`app/api/routes.py`, and relationship visibility in
`frontend/src/routes/transfers.tsx` and Activity annotations. Prefer a small named
evidence/result type over scattered boolean exceptions.

Do not expand this task into fuzzy reconciliation, ML scoring, FX conversion,
fee-aware matching, or many-to-many allocation. Those require separate product
contracts because they cannot meet the same automatic precision guarantee.

Suggested role: backend/accounting engineer, followed by PI-29's independent gate.

## Execution Log

- 2026-09-13: Task created; blocked until PI-27's red contract is reviewed and
  frozen.
