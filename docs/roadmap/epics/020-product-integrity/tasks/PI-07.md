---
id: PI-07
epic: 020-product-integrity
title: Resolve the diagnosed card-payment Sankey gap
status: todo
dependencies: [PI-06, PI-19]
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [ ] Implement the smallest correction that addresses PI-06's demonstrated cause:
  fix a shared-accounting regression if one exists, or close the concrete
  detection/review/history workflow gap if the rows are unresolved.
- [ ] A confirmed repayment between linked checking and linked credit-card accounts
  adds zero household income/spending, does not appear as an expense category in
  Sankey, and leaves the underlying card purchases counted once.
- [ ] If PI-06 identifies unresolved candidates as the cause, the product makes
  their counted status and correction path understandable without silently
  confirming or hiding them.
- [ ] If the counterpart/history is missing, the experience describes the coverage
  limitation and does not claim a transfer that cannot be established.
- [ ] The fix preserves conservative same-amount collision behavior, confirmed-pair
  reversibility, category/refund semantics, and agreement across all aggregate and
  Activity evidence paths.
- [ ] Add the exact hand-checkable regression discovered in PI-06 plus explicit
  confirmed-versus-unresolved Sankey assertions; demonstrate failure before the fix
  when a defect exists and record green full-suite results afterward.
- [ ] Re-run transfer detector/API, shared accounting, Sankey, category, and frontend
  presentation tests; update current operating documentation only if the workflow
  or limitation changes.
- [ ] Make PI-19's diagnosis-specific regression cases pass unchanged. If PI-06
  changes the contract after those cases are accepted, stop and escalate the test
  revision rather than changing it as part of implementation.

## Technical Context

Do not create a Sankey-only category suppression rule. Likely correction areas,
depending on evidence, are `app/services/transfer_detector.py`, transfer review in
`frontend/src/routes/transfers.tsx`, transaction relationship visibility, shared
accounting predicates, or `analytics_cashflow_sankey()` and its tests. Preserve
PI-06's audit artifact as the rationale for the chosen path.

Suggested role: engineer appropriate to the diagnosed layer, followed by independent
accounting review.

## Execution Log

- 2026-09-08: Task created; blocked until PI-06 identifies a concrete cause.

### 2026-09-08 — TDD decomposition

PI-19 is created only after PI-06 records the actual failure mode. PI-20 verifies
the correction and may repair implementation code but may not revise PI-19's oracle.
