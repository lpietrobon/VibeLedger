---
id: PI-17
epic: 020-product-integrity
title: Pin duplicate-correction interaction behavior with failing tests
status: done
dependencies: [PI-16]
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Add deterministic failing React/API-client interaction cases before PI-05
  implementation for selecting exactly two rows, choosing canonical, confirming,
  rendering relationship state, reversing, feedback, and cache refresh/reload.
- [x] Cover desktop and mobile selection paths, invalid selections, keyboard use,
  and a legitimate similar-looking pair that receives no automatic duplicate state.
- [x] Declare literal visible labels, endpoint payloads, and fixture effects from
  PI-03/PI-16; record a red command/result and rationale under
  `artifacts/PI-17-duplicate-ui-tdd.md`.
- [x] Freeze the reviewed cases as PI-05's interaction contract. Subsequent work may
  not alter them without escalation.

## Technical Context

Use current `TransactionsPage`, `TransactionRow`, `AnnotationSheet`, API client, and
cache-invalidation tests. Do not mock away the client payload or test only a local
selection state.

Suggested role: frontend test engineer.

## Execution Log

- 2026-09-08: Task created; blocked on PI-16's verified backend contract.
- 2026-09-08: Started after PI-16 verification. Read the roadmap conventions,
  duplicate contract, immutable PI-15 tests, PI-16 audit, and current
  TransactionsPage/TransactionRow/AnnotationSheet/client/cache test seams.
- 2026-09-08: Added immutable client wire-contract tests and desktop/mobile
  interaction tests in
  [PI-17 duplicate UI TDD](../artifacts/PI-17-duplicate-ui-tdd.md). The tests
  freeze exact labels, canonical/duplicate IDs, GET/POST/DELETE requests,
  confirmation language, invalid-selection feedback, cache invalidation, and
  the no-automatic-hiding guard. No production UI, backend code, PI-03, PI-15,
  or PI-16 files were changed.
- 2026-09-08: Red command `cd frontend && npm test -- --run
  src/lib/api/duplicate-correction-tdd.test.ts
  src/routes/duplicate-correction-tdd.test.tsx` produced **7 failed, 1
  passed**. The seven failures are the intentionally absent client/UI
  behavior; the passing case is the preserved similar-charge no-inference
  guard.
- 2026-09-08: Control frontend suite excluding PI-17 passed (**44 tests**).
  `npm run typecheck` and `npm run build` passed. `git diff --check` passed and
  the roadmap scanner reported 0 issues. PI-17 is complete and unblocks PI-05.
