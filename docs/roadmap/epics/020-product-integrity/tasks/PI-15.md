---
id: PI-15
epic: 020-product-integrity
title: Pin duplicate-accounting and lifecycle behavior with failing tests
status: done
dependencies: [PI-03]
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Turn PI-03's accepted duplicate contract into literal, hand-calculated failing
  backend/API cases before PI-04 changes production code.
- [x] Cover canonical/duplicate creation, reversal, invalid/overlapping pairs, two
  legitimate identical-looking charges, aggregates/categories/Sankey/recurring and
  search evidence, plus posted/refund/pending/transfer interactions.
- [x] Cover source modification/removal, relink/fingerprint behavior, orphan cleanup,
  and idempotent schema evolution where PI-03 specifies a durable outcome.
- [x] Expected totals and IDs are declared without calling the product calculation
  being tested; record red command/results and fixture arithmetic in
  `artifacts/PI-15-duplicate-tdd.md`.
- [x] After review, freeze these cases as PI-04's contract. PI-04/PI-16 may not
  weaken, delete, or rewrite them; a required test revision is escalated.

## Technical Context

Use the small CF-02/CF-10 fixture style and existing sync/transfer lifecycle suites.
There is currently no duplicate relationship, so tests should fail because the API
or relationship semantics do not yet exist—not by relying on an unimplemented UI.

Suggested role: backend reliability engineer with accounting reviewer.

## Execution Log

- 2026-09-08: Task created; blocked on PI-03's accepted duplicate contract.

### 2026-09-08 — implementation started

PI-03 is accepted. Work started by freezing the independent fixture and
backend/API cases before PI-04 production changes.

### 2026-09-08 — contract implemented and red-verified

Added the independent six-row fixture in
`tests/duplicate_correction_fixture.py` and immutable backend/API contract
tests in `tests/test_pi15_duplicate_tdd.py`. The cases cover the PI-03
100/74/26 -> 100/49/51 oracle, exact duplicate exclusion and reversal,
invalid/overlapping selections, legitimate identical charges, provider replay,
material modification, item removal/orphan-safe history, and unambiguous
relink. The API boundary and arithmetic are recorded in
[the PI-15 TDD artifact](../artifacts/PI-15-duplicate-tdd.md).

Verification:

- `python -m py_compile tests/duplicate_correction_fixture.py tests/test_pi15_duplicate_tdd.py` — passed.
- `git diff --check` — passed.
- `python -m pytest -q tests/test_pi15_duplicate_tdd.py` — **17 failed, 1 passed, 2 warnings** against the pre-PI-04 application. Failures are expected feature-red responses (`404` for the absent duplicate-correction resource or required `400` validation); no collection/setup errors occurred.

No production code or PI-03 contract files were changed. This freezes the test
oracle for PI-04/PI-16; any requested expectation change requires escalation.
