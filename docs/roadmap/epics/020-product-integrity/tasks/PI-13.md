---
id: PI-13
epic: 020-product-integrity
title: Pin category drilldown semantics with failing tests
status: done
dependencies: []
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Before changing production drilldown behavior, add deterministic failing
  tests for Overview and Spending category activation using one hand-checkable
  fixture with charges, a refund, pending activity, an unconfirmed candidate, a
  confirmed transfer, and more than one Activity evidence page.
- [x] State literal expected destination fields and expected transaction IDs/signed
  expense total for each source aggregate. Assert server reporting bounds, not a
  browser-derived current date.
- [x] Cover equivalent Overview/Spending links, configured base path, exact effective
  category behavior, `is:spend`, inclusive dates, stable sort, keyboard activation,
  and the Activity request/hydration path.
- [x] Record a red command/result against the pre-PI-02 implementation and save any
  fixture rationale under `artifacts/PI-13-category-drilldown-tdd.md`.
- [x] After review, identify the test files and assertions as the accepted PI-02
  contract. Implementation and verifier tasks may add tests, but may not edit these
  cases; an incorrect or incomplete contract must be escalated.

## Technical Context

Start from the shared `CategoryComparison` component, Overview/Spending routes,
Activity URL hydration, `GET /transactions`, and the CF-07 accounting fixture/test
patterns. This is a behavioral specification task: no production code and no
passing-by-construction mock that duplicates the implementation.

Suggested role: test/reliability engineer with accounting reviewer.

## Execution Log

- 2026-09-08: Task created; the test contract must be reviewed and frozen before
  PI-02 implementation begins.
- 2026-09-11: Added the independent fixture/oracle and frozen backend/API and
  route-level frontend tests. The pre-PI-02 frontend contract is intentionally
  red because Overview does not yet provide category activation; backend
  fixture/search/category checks pass. See
  `artifacts/PI-13-category-drilldown-tdd.md`.
