---
id: PI-23
epic: 020-product-integrity
title: Pin the accepted Sankey composition with failing tests
status: done
dependencies: [PI-07, PI-09, PI-22]
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [x] Convert PI-09's selected design into deterministic failing graph/rendering
  cases before PI-10 implementation, including the normal surplus, exact-balance,
  deficit, refund-credit, refund-only, zero-income, and zero-spending cases.
- [x] Assert literal visible nodes/links or companion explanation, stable bucket and
  category keys, non-negative drawable flows, and CF-14 arithmetic identities.
- [x] Assert the normal case is simpler according to PI-09's concrete decision, not
  merely a rearranged “Available cash” hub with the same persistent clutter.
- [x] Record red command/results and fixture arithmetic in
  `artifacts/PI-23-sankey-composition-tdd.md`; use server responses as inputs but
  do not reproduce monetary calculations in the client test oracle.
- [x] Freeze the reviewed cases for PI-10. Implementation/verifier work may not
  alter them; any design-contract revision is escalated to the epic owner.

## Technical Context

This task starts only after PI-09's design decision, PI-07's accounting correction,
and PI-22's readability verification. Reuse CF-14 fixtures and current Sankey test
patterns where they express the selected product contract.

Suggested role: frontend/accounting test engineer.

## Execution Log

- 2026-09-08: Task created; blocked on design and prerequisite verification.

### 2026-09-11 — frozen red contract

- Added `frontend/src/components/finance/charts/SankeyChart-composition-tdd.test.tsx`
  with synthetic server-response fixtures for normal surplus, exact balance,
  deficit, refund credit, refund-only, zero-income, zero-spending, net-zero, and
  empty periods.
- The frozen assertions inspect literal rendered node keys, link titles and
  amounts, non-negative drawable values, the empty-period explanation, and
  expanded category/income-source identity paths. The normal path explicitly
  forbids `Available cash`; exception paths require the named allocation stage.
- Focused red command:

  ```text
  cd frontend && npm test -- --run \
    src/components/finance/charts/SankeyChart-composition-tdd.test.tsx
  ```

  Result: **12 tests; 9 failed, 3 passed**, as expected against the current
  always-hub implementation. The runner also reports existing React `act(...)`
  warnings and NaN SVG warnings for the old all-zero graph.
- Existing frontend tests excluding PI-23: **63 passed**. Relevant backend
  Sankey/accounting tests: **68 passed** with 2 existing deprecation warnings.
  `npm run typecheck` and `npm run build` passed. No production code or prior
  frozen TDD contract was changed.
- `git diff --check` passed. The roadmap scanner must report zero issues after
  this task metadata and artifact are included.
