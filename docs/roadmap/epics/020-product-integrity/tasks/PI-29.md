---
id: PI-29
epic: 020-product-integrity
title: Independently verify generic transfer auto-reconciliation
status: todo
dependencies: [PI-28]
superseded_by: null
assigned_agent: null
created: 2026-09-13
---

## Acceptance Criteria

- [ ] A verifier other than the PI-28 implementer runs PI-27 unchanged on the
  identified integrated commit and confirms its assertions and fixtures were not
  modified.
- [ ] Independently hand-reconcile the positive account matrix and every collision,
  missing-evidence, mutation, rejection, and abstention fixture. Confirm that
  account type is neither a whitelist nor sufficient evidence by itself.
- [ ] Adversarially permute insertion order and add same-amount competitors on both
  sides. Verify that only a mutually unique, strongly evidenced pair is
  auto-confirmed and no outcome depends on database IDs or iteration order.
- [ ] Verify full lifecycle and accounting parity after ordinary sync, historical
  sync, redetection, source modification/removal, rejection, reversal, and manual
  confirmation. Reconcile literal Sankey, aggregate, and Activity totals.
- [ ] Confirm automatic decisions remain inspectable and reversible, while weak or
  unmatched movements remain counted with an honest review/coverage state.
- [ ] The verifier may repair production code and add separate adversarial coverage
  but must not edit PI-27. Escalate any required contract change.
- [ ] Record the tested commit, immutable-test comparison, commands, literal
  expected/actual evidence, code-only fixes, and verdict under
  `artifacts/PI-29-generic-transfer-reconciliation-verification.md`.

## Technical Context

Treat false exclusion as the highest-severity failure: it silently removes real
income or spending. Also reject an implementation that preserves precision only by
leaving the positive cross-account fixtures as manual candidates; the task exists
to remove routine confirmation work when deterministic evidence is complete.

Suggested role: independent accounting/reconciliation verifier.

## Execution Log

- 2026-09-13: Task created; blocked on PI-28.
