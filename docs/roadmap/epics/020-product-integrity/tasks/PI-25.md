---
id: PI-25
epic: 020-product-integrity
title: Pin current-year-aware date formatting with failing tests
status: todo
dependencies: []
superseded_by: null
assigned_agent: null
created: 2026-09-08
---

## Acceptance Criteria

- [ ] Before PI-11 changes the shared formatter, add fixed-reference-date failing
  cases for same-year, prior-year, future-year, year boundary, leap-day, invalid,
  missing, and date-only timezone-safe input behavior.
- [ ] Assert literal outputs such as `2 Sep` and `2 Sep, 2025`, plus no invalid
  user-facing string, without depending on the execution date or host timezone.
- [ ] Add representative callers for Activity, transaction detail, Transfer
  Detection, and Recurring; identify chart-only formatting explicitly as excluded.
- [ ] Record red command/result and formatter/caller audit in
  `artifacts/PI-25-date-formatting-tdd.md`.
- [ ] Freeze the reviewed test contract for PI-11. Any change to expected format or
  edge-case policy needs escalation rather than an in-place test edit.

## Technical Context

Start at `frontend/src/lib/format.ts::formatDate` and its existing call sites. A
testable reference year should be injected or controlled without introducing a
global clock dependency into ordinary rendering.

Suggested role: frontend test engineer.

## Execution Log

- 2026-09-08: Task created; fixed-clock formatter contract pending.
