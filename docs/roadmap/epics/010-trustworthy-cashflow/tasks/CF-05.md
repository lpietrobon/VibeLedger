---
id: CF-05
epic: 010-trustworthy-cashflow
title: Harden transaction identity and synchronization
status: in-progress
dependencies: [CF-02, CF-04]
superseded_by: null
assigned_agent: null
created: 2026-09-02
---

## Acceptance Criteria

- [ ] Replaying the same provider record or sync page does not create another economic transaction or change totals.
- [ ] Pending-to-posted transitions leave one posted transaction in realized reporting and preserve valid user corrections.
- [ ] Provider changes to posting date, amount, description, or removal update reports and invalidate stale derived matches as appropriate.
- [ ] Failed or interrupted batches can be retried without partial duplication, skipped history, or lost user decisions; verify cursor and transaction atomicity.
- [ ] Deletion and relinking do not leave orphan annotations, transfer/refund relationships, or rejection records attached to unrelated transactions.
- [ ] Adding a linked account or older history makes counterpart reconciliation possible without requiring historical reports to be rebuilt by hand.
- [ ] Assess duplicate account coverage from relinking/repeated linking; prevent silently presenting double-covered totals as reliable, using the smallest supported correction path.
- [ ] Add focused regression tests and document actual provider/history/currency limitations. Do not claim Venmo support from synthetic examples alone.

## Technical Context

Suggested role: ingestion/backend engineer. Inspect `sync_service.py`,
`plaid_client.py`, `txn_fingerprint.py`, model constraints, schema patches, and
item-removal paths. Distinct same-amount purchases are not import duplicates.
Avoid destructive cleanup against real data and avoid matching account identities
by weak fields alone. Any migration must preserve annotations and be repeatable.

Coordinate lifecycle interfaces with CF-06 and CF-07; assign ownership before
editing shared routes/schema files. New provider integrations are separate scope.

## Execution Log

- 2026-09-02: Task created; ingestion hardening and lifecycle verification pending.

### 2026-09-02 — recovered pre-roadmap work

**Ingestion implementation draft recovered.** Earlier ingestion work produced provider-ID replay handling, modified-record/date/hash updates, pending-to-posted row replacement, annotation/fingerprint preservation, category-rule application, stale relationship cleanup, and nested-transaction rollback. Draft tests cover these paths. Source and tests are in [the preserved patch](../artifacts/CF-01-unpublished-draft.patch); fresh suite evidence is linked in [the recovery report](../artifacts/CF-01-recovered-progress.md).

**Blocked / still needed:** the fresh full suite fails `test_sync_all_items_syncs_active_items`: the second item encounters `account belongs to a different linked item`. Resolve the fixture/mock identity contract without weakening safeguards, verify repeated-link/account-coverage semantics, complete lifecycle review, and integrate after CF-02/CF-04. No new connector or live Venmo support was established. Earlier implementer completion of a slice does not close this task.

### 2026-09-02 — execution resumed

Starting the first isolated sync-atomicity correction with a reproduced failure and focused regressions; remaining identity/reconciliation work stays open.

### 2026-09-02 — first implementation slice: atomic failed imports

Added a savepoint around account/balance refresh and transaction application in incremental and historical sync. A failure now rolls back business changes while retaining the error run and unchanged cursor. Four scenarios (new/existing account × incremental/historical) reproduced partial-row commits before the fix, then passed with checks for balances, snapshots, cursor, retained error evidence, successful retry and idempotent replay. [Before: four failures](../artifacts/CF-05-atomicity-before.txt); [after: full suite 167 passed](../artifacts/CF-05-atomicity-after.txt); [84.46% combined coverage](../artifacts/CF-05-atomicity-coverage.json). Changes are limited to `sync_service.py` transaction boundaries and focused regression tests; no transfer matching or category policy changes were imported.

**Still open:** modified-record identity/date updates, pending replacement and annotation preservation, removal/relink cleanup, duplicate account coverage, integration and independent verification. CF-05 remains in progress.

### 2026-09-02 — first slice passed GitHub CI; resume point

Implementation commit `620bbc051f7a0ab9f9162cbd3f3d1118a173ecd7` passed both backend and frontend jobs in [run 33693650681](https://github.com/lpietrobon/VibeLedger/actions/runs/33693650681). The task remains in progress with no active agent assigned. Next: selectively port provider-ID/date updates and pending-to-posted preservation from the preserved draft, add their regressions, and review fingerprint/reconciliation invalidation. Do not import the entire draft or its known scheduler/identity conflict without resolving it. Current published baseline is green; rollback protection is applied application code, not an artifact-only draft.

### 2026-09-03 — lifecycle and identity slice

Provider IDs remain the only import-identity key: replays are no-ops, while genuinely separate same-content purchases remain separate rows. A posted record carrying Plaid's `pending_transaction_id` now updates the existing pending row in place, preserving its annotation and fingerprint rather than briefly creating two economic transactions. Material source changes invalidate transfer/refund matches while retaining notes and manual categories; removed records clear transfer rejections and detach fingerprints for a future relink.

Linked-account ownership is now guarded: a record cannot silently move from one linked item to another. The local Plaid mock now produces isolated account and transaction IDs per access token, so multi-item sync tests model real provider identity instead of sharing one synthetic account. Focused lifecycle tests and the full backend suite passed: **169 tests, 84.72% combined coverage**. The task remains in progress: duplicate account coverage and provider-history limitations still require explicit treatment, and later CF-06/CF-07 work must verify the lifecycle boundaries end to end.
