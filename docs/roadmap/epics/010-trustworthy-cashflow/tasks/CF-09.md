---
id: CF-09
epic: 010-trustworthy-cashflow
title: Finish existing transaction review and recurring workflows
status: blocked
dependencies: [CF-03, CF-05, CF-06, CF-07]
superseded_by: null
assigned_agent: null
created: 2026-09-02
---

## Acceptance Criteria

- [ ] Existing attention counts and review queues lead to the same eligible records, including matches beyond the first fetched page.
- [ ] Users can inspect the evidence for a questionable classification, transfer candidate, or available duplicate/suspicious-charge hint, then correct or dismiss it through a small clear workflow.
- [ ] The UI distinguishes a replayed import from two actual charges; reviewing or dismissing a suspicion does not erase a real transaction or exclude it from totals.
- [ ] Confirm/reject, recategorize, and reviewed-state actions persist through refresh and resync, with visible success/error feedback and correct downstream totals.
- [ ] Existing subscription counts, active/canceled overrides, cadence, and cost estimates are consistent and traceable to eligible posted transaction evidence; label estimates and limited history honestly.
- [ ] Internal transfers and refunds do not become subscriptions; same-size purchases are not declared duplicate services without evidence.
- [ ] Fix gaps in the existing review flow identified by CF-03 and add regression tests. If a requested suspicion type has no existing detection support, document the gap and a separate scope decision rather than implying it is detected.
- [ ] Keep the scope to useful existing review and recurring behavior; no new trial-conversion, forecasting, generalized fraud, or optimization subsystem.

## Technical Context

Suggested role: full-stack engineer. Start with transfer review, annotation UI,
needs-attention query mappings, recurring detector/endpoints, and recurring pages.
Use existing queue structures unless evidence shows a small consolidation is
necessary. Coordinate cache refresh and transaction components with CF-08.

This task restores trustworthy existing behavior; the broader FI-01/FI-03/FI-06
feature ideas remain deferred. Basic review visibility is in scope even when a
new detector is not.

## Execution Log

- 2026-09-02: Task created; review and recurring workflow completion pending.

### 2026-09-02 — recovered pre-roadmap work

**Partial review-flow drafts recovered.** Annotation sheets await saves, retain failures visibly, and guard repeated submissions; transfer labeling and mutation cache invalidation were adjusted. Recurring test fixtures now use a deterministic clock, and related backend tests pass. See [the patch inventory and tests](../artifacts/CF-01-recovered-progress.md).

**Blocked / still needed:** prerequisite acceptance, end-to-end attention-count/queue parity, persisted confirm/reject/review actions, subscription count/cadence/override evidence, and an explicit inventory of supported duplicate/suspicion hints. No new duplicate-charge/fraud detector or full recurring-workflow acceptance is implied by the draft. Coordinate shared UI files with CF-08.
