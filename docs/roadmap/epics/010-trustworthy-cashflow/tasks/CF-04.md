---
id: CF-04
epic: 010-trustworthy-cashflow
title: Establish focused regression fixtures and coverage checks
status: in-progress
dependencies: [CF-01, CF-02]
superseded_by: null
assigned_agent: coordinator
created: 2026-09-02
---

## Acceptance Criteria

- [ ] Provide reusable synthetic ledger fixtures with independently specified expected results from CF-02; cover cross-account and cross-period behavior.
- [ ] Make the relevant existing tests deterministic, including recurring/calendar tests that otherwise depend on today's date.
- [ ] Add regression checks for confirmed baseline defects as their fixes land; do not leave the branch permanently red or assert today's buggy output as the intended behavior.
- [x] Establish reproducible backend coverage reporting with the exact measured source set, omissions, statement result, and branch result documented.
- [ ] Set a justified gate around the user's 70–80% backend target while preserving a higher comparable baseline. Do not inflate coverage with arbitrary exclusions or equate backend coverage with whole-product coverage.
- [ ] Run backend checks and the existing frontend behavioral/type/build checks in lightweight CI using mock providers and isolated databases, without production secrets or network calls.
- [ ] Document the local and CI commands and how to add one useful regression scenario without duplicating the implementation.

## Technical Context

Suggested role: test/reliability engineer. Reuse pytest and the frontend's existing
test tooling. Focus extra branch coverage on financial decisions and lifecycle
transitions; avoid blanket UI snapshot coverage or a new testing framework.

This task establishes the harness and initial gate. Fix owners add scenario tests
with their code; CF-12 measures the final integrated coverage and verifies the
gate has not been weakened. Coordinate shared fixture edits with those owners.

## Execution Log

- 2026-09-02: Task created; fixtures, measured coverage gate, and CI checks pending.

### 2026-09-02 — recovered pre-roadmap work

**Local test/CI drafts exist.** The patch includes an 80% combined backend coverage gate, a GitHub Actions workflow, deterministic recurring fixtures, and additional accounting/dashboard/provider/sync tests. Fresh checks on the recovered snapshot: **210 passed, 1 failed**; backend statement **88.22%**, branch **78.19%**, combined **86.05%**, over `app/` with no excluded lines. Frontend: **48 tests in 5 files passed**, typecheck and build passed. See [commands and baseline comparison](../artifacts/CF-01-recovered-progress.md) and [coverage metadata](../artifacts/CF-04-coverage-summary.json).

**Blocked / still needed:** CF-01/CF-02 acceptance, resolve the scheduler failure with CF-05, review fixture assertions, and land/run the actual CI workflow. Passing the coverage threshold is not a green test suite. The workflow is an unapplied draft, not an enforced GitHub gate. Preserve the higher measured baseline.

### 2026-09-02 — delegated after prerequisites closed; interrupted

CF-01/CF-02 are done (`5c7c39e`, `d4e6871`). One bounded `test_foundation` helper was assigned the deterministic recurring clock fix, five fake-SDK provider tests, reusable CF-02 fixture support, coverage configuration, and a lightweight backend/frontend CI workflow. The workspace became unavailable before a completion report or test output was returned; the helper is no longer running. **No new implementation or test pass from this attempt has been verified or pushed.** Earlier 210-pass/1-fail figures describe the previously preserved whole draft, not this proposed small slice. [Exact scope and recovery plan](../artifacts/CF-04-resume-plan.md). Resume from the committed draft artifact if local files are gone; keep the initial CI slice green without pulling in unreviewed accounting/sync changes. Current blocker: execution/test environment availability.

### 2026-09-02 — workspace recovered; validation resumed

Recovered the test helper changes and its 163-pass/84.14% local run. Coordinator reviewing the exact slice, repairing missing fixture transfer/refund relationships, and rerunning checks before publication. Prior interruption and original draft evidence remain historical.

### 2026-09-02 — local foundation verified; CI publication next

Recovered and corrected the bounded helper slice. The reusable fixture now includes four confirmed pairs, the refund link, pending state, currency evidence and stable IDs after commit; a direct smoke check validated that setup. Clock-dependent recurring tests are deterministic, five fake-SDK provider tests pass, and the proposed CI workflow uses mocks/isolated databases. The gate is **81.34% combined**, retaining the baseline rather than rounding it down. Fresh full backend result: **163 passed**, **84.14% combined coverage**, no excluded lines; frontend tests/typecheck/build pass. [Backend log](../artifacts/CF-04-foundation-backend-tests.txt), [frontend log](../artifacts/CF-04-foundation-frontend-checks.txt), [coverage](../artifacts/CF-04-foundation-coverage.json), and [portable commands](../../../../testing.md). No application behavior changes are included. Task remains in progress until independent slice review and actual GitHub CI checks are recorded.

Independent `cf04_review` helper found no issues in CI installation, mock isolation, baseline gate, deterministic clock, or fixture relationships/IDs. Its reference to the prior scheduler failure applies to the separate unlanded application draft; the complete selected-slice run passes all 163 tests including scheduler tests.
